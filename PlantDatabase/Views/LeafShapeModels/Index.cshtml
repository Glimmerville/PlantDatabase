@model IEnumerable<PlantDatabase.Models.LeafShapeModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LeafShapeStyle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LeafShapeStyle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LeafShapeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LeafShapeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LeafShapeID })
        </td>
    </tr>
}

</table>
