@model IEnumerable<PlantDatabase.Models.BloomColorModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FlowerColor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FlowerColor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BloomColorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BloomColorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BloomColorID })
        </td>
    </tr>
}

</table>
