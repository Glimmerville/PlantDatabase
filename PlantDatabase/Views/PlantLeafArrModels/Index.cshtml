@model IEnumerable<PlantDatabase.Models.PlantLeafArrModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlantLeafArr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlantLeafArr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlantLeafArrID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlantLeafArrID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlantLeafArrID })
        </td>
    </tr>
}

</table>
